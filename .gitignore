# App artifacts
/_build
/db
/deps
/*.ez

# Used by Elixir-LS plugin
/.elixir_ls

# Generated on crash by the VM
erl_crash.dump

# Files matching config/*.secret.exs pattern contain sensitive
# data and you should not commit them into version control.
#
# Alternatively, you may comment the line below and commit the
# secrets files as long as you replace their contents by environment
# variables.
/config/*.secret.exs

# Environment variables + other files
.DS_Store
.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# We don't need to commit bundled assets. These are provided by Webpack.
/priv/static

# We're node too!
node_modules/

# We're yarn! So don't, like, use the npm lockfile!
package-lock.json

# Hey I just wanted to take this moment to say, at the bottom of this gitignore,
# be nice to someone today. It doesn't matter who, but you gotta be nice to
# someone, and like actually nice, not passive aggressively nice. You gotta do
# it because the gitignore told you to.
